```
usage: wordle.py [-h] [-i INDEX] [-a MAX_ATTEMPTS]

optional arguments:
  -h, --help            show this help message and exit
  -i INDEX, --index INDEX
                        Select puzzle by index
  -a MAX_ATTEMPTS, --attempts MAX_ATTEMPTS
                        Set max attempts
```